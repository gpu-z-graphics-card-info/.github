# .github
GPU-Z surfaces GPU name, die size, driver version, and BIOS version; GPU-Z sensor tab tracks clocks, temps, and power when the driver exposes them; GPU-Z Vulkan OpenCL lists advertise compute stacks for troubleshooting game launches.
